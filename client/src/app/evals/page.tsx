/* /evals — thin route entry. All logic lives in the colocated dashboard view. */
import { EvalDashboardView } from "./_components/EvalDashboardView";

export default function EvalsPage() {
  return <EvalDashboardView />;
}
