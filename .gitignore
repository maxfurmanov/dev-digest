node_modules/
dist/
# agent-runner ships as a JS GitHub Action — its bundled dist/ MUST be committed
# (GitHub runs action.yml `main: dist/index.js` as-is, with no build step).
!agent-runner/dist/
!agent-runner/dist/**
build/
.next/
out/
coverage/
*.tsbuildinfo
.env
.env.local
.DS_Store
.idea/
.turbo/
.pnpm-store/
*.log
.devdigest/cache/
clones/
playwright-report/
test-results/

# emdash worktree config (local)
.emdash.json

# generated locally by the Claude Code preview tools
.claude/launch.json

# local lesson planning notes
/client/pnpm-workspace.yaml
/server/pnpm-workspace.yaml

# old skill-eval harness run data — same nature as evals/results/, not source
.claude/skill-eval-runs/
